# print(dbowells_ic['well_id'].count())
# a = dbowells_ic['well_id'].to_list()
# a
# # 1927

# # Compare two lists
# set(b)

missing_wellid = set(b).difference(a)
missing_wellid

# print(df_explo_dbowells['well_id'].count())
# b = df_explo_dbowells['well_id'].to_list()

# # Compare columns between dbowells_ic and df_explo_dbowells
# # So this is missing (to make up to 51 cols): rte, rig_elevation, sub_block, basin, name2, strat_schemes, grnd_elev, f_block, f_ipid, f_comment, f_province, f_county, f_state, f_section, f_Township, f_range